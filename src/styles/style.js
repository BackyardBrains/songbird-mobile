import {StyleSheet} from 'react-native'

export default StyleSheet.create({

    container: {
        marginTop: 15,
        marginBottom: 15,
        alignItems: 'center'
      },
    
      header: { 
        fontSize: 30
      },
    
      statusText: {
        fontSize: 30,
        marginTop: 5,
        marginBottom: 5
      },

      smallText: {
        fontSize: 15
      }

});